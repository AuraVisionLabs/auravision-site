---
import "../styles/global.css";

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}

const {
  title,
  description = "Auravision — AI-powered solutions for forward-thinking businesses.",
  ogImage = "/og-default.jpg",
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <link rel="canonical" href={canonicalURL} />

    <!-- Google Fonts: Questrial (titles) + Open Sans (body) -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600&family=Questrial&display=swap"
      rel="stylesheet"
    />

    <title>{title} | Auravision</title>
    <meta name="description" content={description} />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={`${title} | Auravision`} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />

  </head>
  <body class="bg-black text-white antialiased">
    <slot />

    <script>
      import netlifyIdentity from "netlify-identity-widget";

      netlifyIdentity.init();

      const hash = window.location.hash;
      const isAuthRedirect =
        hash.includes("confirmation_token") ||
        hash.includes("recovery_token") ||
        hash.includes("invite_token");

      netlifyIdentity.on("init", (user: ReturnType<typeof netlifyIdentity.currentUser>) => {
        if (!user && isAuthRedirect) {
          netlifyIdentity.open();
        }
        if (!user) {
          netlifyIdentity.on("login", () => {
            document.location.href = "/admin/";
          });
        }
      });
    </script>
  </body>
</html>
