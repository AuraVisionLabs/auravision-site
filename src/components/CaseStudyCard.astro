---
import { ArrowRight } from "@lucide/astro";

interface Props {
  slug: string;
  title: string;
  client: string;
  clientLogo?: string;
  summary: string;
  coverImage?: string;
  headingLevel?: "h2" | "h3";
  class?: string;
}

const {
  slug,
  title,
  client,
  clientLogo,
  summary,
  coverImage,
  headingLevel = "h3",
  class: className,
} = Astro.props;

const Heading = headingLevel;
---

<a
  href={`/case-studies/${slug}`}
  class:list={[
    "group flex flex-col rounded-2xl border border-white/10 bg-white/[0.03] backdrop-blur-sm overflow-hidden transition-all duration-300 hover:border-cyan-400/30 hover:bg-white/[0.06] hover:shadow-[0_0_40px_rgba(8,145,178,1.0)]",
    className,
  ]}
>
  {coverImage && (
    <div class="aspect-video overflow-hidden bg-white/5">
      <img
        src={coverImage}
        alt={title}
        class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
      />
    </div>
  )}
  <div class="p-3 sm:p-6 flex flex-col flex-grow">
    <Heading class="font-title text-sm sm:text-xl tracking-tight mb-1 sm:mb-3 text-white group-hover:text-accent transition-colors duration-300 line-clamp-2">
      {title}
    </Heading>

    <p class="hidden sm:block text-sm text-white/50 leading-relaxed mb-5 line-clamp-3">
      {summary}
    </p>

    <div class="mt-auto flex items-center justify-between gap-2 sm:gap-6 sm:h-12">
      <span class="shrink-0 inline-flex items-center gap-1 sm:gap-2 text-[10px] sm:text-xs font-medium px-2 py-1 sm:px-4 sm:py-2 rounded-full bg-white/10 text-white/70 group-hover:bg-accent/20 group-hover:text-accent transition-all duration-300 whitespace-nowrap">
        Read story
        <ArrowRight class="w-3 h-3 sm:w-3.5 sm:h-3.5 transition-transform duration-300 group-hover:translate-x-0.5" />
      </span>

      {clientLogo && (
        <img
          src={clientLogo}
          alt={client}
          class="max-h-8 sm:max-h-12 max-w-[60px] sm:max-w-[120px] w-auto object-contain shrink min-w-0 opacity-60 [filter:brightness(0)_invert(1)]"
        />
      )}
    </div>
  </div>
</a>
