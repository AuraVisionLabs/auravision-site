---
import { ArrowRight } from "@lucide/astro";

interface Props {
  title: string;
  subtitle: string;
  description: string;
  linkText: string;
  linkHref?: string;
  reversed?: boolean;
}

const {
  title,
  subtitle,
  description,
  linkText,
  linkHref = "/contact",
  reversed = false,
} = Astro.props;
---

<section class="py-24 md:py-32">
  <div class="mx-auto max-w-site px-6 lg:px-8">
    <div class="grid items-center gap-16 md:grid-cols-2">
      <div class:list={[reversed && "order-1 md:order-2"]}>
        <h2 class="font-title text-4xl leading-tight tracking-tight md:text-5xl lg:text-6xl text-gradient" data-text={title}>
          {title}
        </h2>
        <p class="mt-4 text-xl text-white font-medium md:text-2xl">
          {subtitle}
        </p>
        <p class="mt-6 text-lg leading-relaxed text-white">
          {description}
        </p>
        <a href={linkHref} class="group mt-8 inline-flex items-center gap-2 text-sm font-medium text-cyan-300 transition-colors hover:text-purple-300">
          {linkText}
          <ArrowRight class="h-4 w-4 text-cyan-500 transition-colors group-hover:text-purple-300" stroke-width={2} />
        </a>
      </div>
      <div class:list={[reversed && "order-2 md:order-1"]}>
        <slot name="visual">
          <div class="flex items-center justify-center rounded-2xl border border-white/5 bg-white/[0.03] backdrop-blur-sm aspect-video">
            <p class="text-sm text-white/20">Visual coming soon</p>
          </div>
        </slot>
      </div>
    </div>
  </div>
</section>
